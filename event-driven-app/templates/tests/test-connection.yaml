apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "event-driven-app.fullname" . }}-test-connection"
  labels:
    {{- include "event-driven-app.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "event-driven-app.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
